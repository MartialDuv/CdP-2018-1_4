dist: trusty
sudo: required

language: node_js
node_js:
  - "10"

services :
  - docker

addons:
  apt:
    packages:
      - docker-ce

notifications:
  email: false

before_install:
  - sudo docker-compose up -d
  - sudo npm install puppeteer
  - sudo npm install jasmine


script:
  - curl localhost:3000
  - yarn test
